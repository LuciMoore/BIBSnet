def run_preBIBSnet(BIBSnet, subject, session):
    print(f"run prebibsnet, sub {subject}: ses {session}")

def run_BIBSnet(BIBSnet, subject, session):
    print(f"run bibsnet, sub {subject}: ses {session}")

def run_postBIBSnet(BIBSnet, subject, session):
    print(f"run postbibsnet, sub {subject}: ses {session}\n")