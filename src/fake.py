def run_preBIBSnet(ArgsAndFiles, subject, session):
    ArgsAndFiles.LOGGER.info(f"run prebibsnet, sub {subject}: ses {session}")

def run_BIBSnet(ArgsAndFiles, subject, session):
    ArgsAndFiles.LOGGER.info(f"run bibsnet, sub {subject}: ses {session}")

def run_postBIBSnet(ArgsAndFiles, subject, session):
    ArgsAndFiles.LOGGER.info(f"run postbibsnet, sub {subject}: ses {session}\n")